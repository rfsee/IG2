services:
  - type: web
    name: ig2-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm run start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: DATA_PROVIDER
        value: postgres
      - key: AUTH_PROVIDER
        value: local
      - key: AUTH_REGISTER_ENABLED
        value: "true"
      - key: AUTH_REGISTER_EMAIL_ALLOWLIST
        sync: false
      - key: AUTH_REGISTER_EMAIL_ALLOWLIST_REGEX
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
      - key: AUTH_SESSION_TTL_DAYS
        value: "3"
      - key: AUTH_PASSWORD_MIN_LENGTH
        value: "10"
      - key: DATABASE_URL
        sync: false
